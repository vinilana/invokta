{
  "name": "@invokta/example-image",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "build": "tsc -b --pretty false",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.test.json --pretty false --noEmit",
    "test": "vitest run test",
    "direct": "node dist/direct.js",
    "devtools:verify": "tsc -b --pretty false && invokta-devtools verify --stdio node --arg dist/mcp-stdio.js --env OPENAI_API_KEY=OPENAI_API_KEY --env ARK_API_KEY=ARK_API_KEY --env GEMINI_API_KEY=GEMINI_API_KEY",
    "cli": "node dist/cli.js",
    "mcp:stdio": "node dist/mcp-stdio.js",
    "mcp:http": "node dist/mcp-http.js"
  },
  "dependencies": {
    "@invokta/cli": "0.8.0",
    "@invokta/core": "0.8.0",
    "@invokta/mcp": "0.8.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@invokta/devtools": "0.8.0",
    "@modelcontextprotocol/sdk": "1.30.0"
  }
}
