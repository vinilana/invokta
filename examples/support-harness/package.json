{
  "name": "@invokta/example-support-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b --pretty false",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.test.json --pretty false --noEmit",
    "test": "vitest run test",
    "devtools:verify": "tsc -b --pretty false && invokta-devtools verify --stdio node --arg ../support-engine/dist/mcp-stdio.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "@invokta/devtools": "0.9.0"
  }
}
