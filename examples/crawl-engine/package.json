{
  "name": "@invokta/example-crawl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "build": "tsc -b --pretty false",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.test.json --pretty false --noEmit",
    "test": "vitest run test",
    "direct": "node dist/direct.js",
    "devtools:verify": "tsc -b --pretty false && invokta-devtools verify --stdio node --arg dist/mcp-stdio.js --env FIRECRAWL_API_KEY=FIRECRAWL_API_KEY",
    "cli": "node dist/cli.js",
    "mcp:stdio": "node dist/mcp-stdio.js",
    "mcp:http": "node dist/mcp-http.js"
  },
  "dependencies": {
    "@invokta/cli": "0.8.2",
    "@invokta/core": "0.8.2",
    "@invokta/mcp": "0.8.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@invokta/devtools": "0.8.2",
    "@modelcontextprotocol/sdk": "1.30.0"
  }
}
